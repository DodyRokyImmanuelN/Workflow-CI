name: MLflow Project Training - Heart Disease

# ===== TRIGGER =====
on:
  # Manual trigger
  workflow_dispatch:

  # Trigger saat push ke main
  push:
    branches:
      - main
    paths:
      - "MLProject/**"
      - ".github/workflows/mlflow-training.yml"

  # Scheduled trigger (setiap hari jam 12:00 UTC)
  schedule:
    - cron: "0 12 * * *"

# ===== PERMISSIONS =====
permissions:
  contents: write
  actions: read
  packages: write

# ===== JOBS =====
jobs:
  mlflow-training:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Step 2: Setup Python
      - name: Setup Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          echo "✅ Dependencies installed"

      # Step 4: Setup MLflow tracking directories
      - name: Setup MLflow tracking
        run: |
          mkdir -p mlruns
          mkdir -p artifacts
          echo "✅ Directories created"

      # Step 5: Prepare dataset for ML-Project
      - name: Prepare dataset
        run: |
          # Copy preprocessed dataset to ML-Project folder
          if [ -f "preprocessing/heart_preprocessing.csv" ]; then
            cp preprocessing/heart_preprocessing.csv ML-Project/
            echo "✅ Dataset copied to ML-Project/"
          elif [ -f "heart_preprocessing.csv" ]; then
            cp heart_preprocessing.csv ML-Project/
            echo "✅ Dataset copied to ML-Project/"
          else
            echo "⚠️ Warning: Preprocessed dataset not found"
            echo "   Workflow will proceed but may fail during training"
          fi

      # Step 6: Run MLflow Project
      - name: Run MLflow Project (CI Retraining)
        run: |
          export MLFLOW_TRACKING_URI="file://$(pwd)/mlruns"
          mlflow run ML-Project \
            --env-manager=local \
            -P data_path=heart_preprocessing.csv \
            -P n_estimators=200 \
            -P max_depth=15

      # Step 7: Get latest MLflow run ID
      - name: Get latest MLflow run ID
        id: get_run_id
        run: |
          # Check if latest_run_id.txt exists (created by modelling.py)
          if [ -f "ML-Project/latest_run_id.txt" ]; then
            RUN_ID=$(cat ML-Project/latest_run_id.txt)
            echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
            echo "✅ Run ID from file: $RUN_ID"
          else
            # Fallback: Query MLflow
            export MLFLOW_TRACKING_URI="file://$(pwd)/mlruns"
            RUN_ID=$(python -c "
          import mlflow
          client = mlflow.tracking.MlflowClient()
          experiment = client.get_experiment_by_name('HeartDisease_Classification_ML-Project')
          if experiment is None:
              raise Exception('Experiment not found')
          runs = client.search_runs(experiment.experiment_id, order_by=['start_time DESC'], max_results=1)
          if len(runs) == 0:
              raise Exception('No runs found')
          print(runs[0].info.run_id)
          ")
            echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
            echo "✅ Run ID from MLflow: $RUN_ID"
          fi

      # Step 8: Collect artifacts
      - name: Collect artifacts
        run: |
          echo "📦 Collecting artifacts..."

          # Copy MLflow runs
          if [ -d "mlruns" ]; then
            cp -r mlruns artifacts/ || true
            echo "✅ MLflow runs copied"
          fi

          # Copy ML-Project artifacts
          if [ -d "ML-Project/artifacts" ]; then
            cp -r ML-Project/artifacts/* artifacts/ 2>/dev/null || true
            echo "✅ ML-Project artifacts copied"
          fi

          # List all artifacts
          echo "📋 Artifacts collected:"
          find artifacts/ -type f

      # Step 9: Upload artifacts to GitHub
      - name: Upload artifacts to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-training-artifacts-${{ github.sha }}
          path: |
            artifacts/
            mlruns/
          retention-days: 30

      # Step 10: Commit artifacts to repository
      - name: Commit artifacts to repository
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"

          # Only commit if there are changes
          git add artifacts/ || true

          if git diff --staged --quiet; then
            echo "⏭️ No changes to commit"
          else
            git commit -m "🤖 Auto: Update MLflow artifacts [Run: ${{ steps.get_run_id.outputs.run_id }}] - $(date +'%Y-%m-%d %H:%M:%S')" || echo "⚠️ Commit skipped"
            git push origin main || echo "⚠️ Push skipped (might need force push)"
          fi

      # Step 11: Job Summary
      - name: Job Summary
        run: |
          echo "## ✅ MLflow Project Training Complete - ADVANCED" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**🎯 Project**: Heart Disease Classification" >> $GITHUB_STEP_SUMMARY
          echo "**🎯 Level**: ADVANCED (4 pts)" >> $GITHUB_STEP_SUMMARY
          echo "**⏰ Timestamp**: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "**🔗 MLflow Run ID**: \`${{ steps.get_run_id.outputs.run_id }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**📦 Commit SHA**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Completed Tasks" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Heart Disease Model Training (MLflow Project)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Artifacts Saved to GitHub" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Model Metrics Logged to MLflow" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Visualizations Generated" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Generated Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Trained Model (heart_disease_model.pkl)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Confusion Matrix (heart_confusion_matrix.png)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Feature Importance (heart_feature_importance.png)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Classification Report (heart_classification_report.txt)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ MLflow Tracking Data (mlruns/)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📥 Download Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "Artifacts are available for download in the Actions tab:" >> $GITHUB_STEP_SUMMARY
          echo "1. Go to **Actions** tab" >> $GITHUB_STEP_SUMMARY
          echo "2. Select this workflow run" >> $GITHUB_STEP_SUMMARY
          echo "3. Scroll down to **Artifacts** section" >> $GITHUB_STEP_SUMMARY
          echo "4. Download: \`mlflow-training-artifacts-${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎓 Kriteria Advanced Terpenuhi" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Workflow CI dengan GitHub Actions" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Artifacts tersimpan di GitHub repository" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ MLflow experiment tracking terintegrasi" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Automatic artifact collection dan upload" >> $GITHUB_STEP_SUMMARY
